# Disable Wi-Fi power saving. The mt7921e driver defaults power-save ON
# (NetworkManager wifi.powersave=3 = use driver default), which aggravates
# the MT7922 drop/latency issue. 2 = disable.
[connection]
wifi.powersave = 2
