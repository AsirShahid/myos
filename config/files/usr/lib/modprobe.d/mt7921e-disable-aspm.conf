# Workaround for MediaTek MT7922 (mt7921e) PCIe ASPM bug.
# Symptom: repeated "Connection to AP lost" drops + BT/USB resets,
# causing high latency / packet loss despite a strong signal.
# disable_aspm=1 keeps the card out of the buggy low-power link states.
options mt7921e disable_aspm=1
