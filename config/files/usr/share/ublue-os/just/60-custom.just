# vim: set ft=make :
# Custom ujust commands for myos.
#
# The base image's justfile imports this file automatically — add custom
# recipes here. Don't duplicate or copy recipes from the base image's own
# just files (bluefin-*.just etc.); those are shipped and updated by the
# base image itself.
